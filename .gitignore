# 홈 디렉터리 개인 파일 제외
.Xauthority
.bash_history
.bash_logout
.bashrc
.profile
.gitconfig
.sudo_as_admin_successful

# 캐시 및 런타임 디렉터리
.cache/
.local/
.npm/
.config/

# SSH 키 (보안)
.ssh/

# Claude 내부 파일
.claude.json
.claude/

# Kubernetes join 토큰 (보안 — 절대 커밋 금지)
scripts/k8s-install/join-tokens/

# kubeconfig (보안)
*.kubeconfig
admin.conf
